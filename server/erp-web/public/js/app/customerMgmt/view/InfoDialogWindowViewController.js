/*
 * File: app/view/MyViewportViewController.js
 *
 * This file was generated by Sencha Architect version 3.5.1.
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 6.0.x library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 6.0.x. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

Ext.define('CustomerMgmt.view.InfoDialogWindowViewController', {
  extend: 'Ext.app.ViewController',
  alias: 'controller.infodialogwindow',

  requires: [],
  /**
   * 初始化
   */
    init: function() {
      this.getViewModel().set("formData", {
        memberNick:"",
        shopId:"",
        memo:""
      });
    },
  /**
   *确定
   */
  onButtonSaveClick: function (button, e, eOpts) {
    var viewCtr = this;
    var form = this.lookupReference("form");
    if (!form.isValid()) {
      TipsUtil.showTips("提示", "信息填写不正确，请检查后重新操作", TipsUtil.WARING);
      return;
    }
    var view = this.getView();
    var viewModel = this.getViewModel();
    var form = this.lookupReference("form");
    var values = form.getForm().getValues();
    var formData = viewModel.get("formData");
    var requestFormData = Ext.clone(formData);
    var url = '/api/general';
    if (requestFormData.customerId) {
      url += '/customer/update/' + requestFormData.customerId;
    } else {
      url += '/customer/add';
    }
    Ext.Ajax.request({
      clientValidation: false,
      url: url,
      params: values,
      success: function (response, opts) {
        Ext.data.StoreManager.getByKey('MainViewportStore').reload();
        view.close();
      },
      failure: function (response, options) {
        Ext.MessageBox.alert('错误', action);
      }
    })
  },

  /**
   * 取消
   */
  onButtonCancelClick: function () {
    this.getView().close();
  }

});
